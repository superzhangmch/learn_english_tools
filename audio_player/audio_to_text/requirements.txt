# --- audio_to_text dependencies ---
# Whisper on Apple Silicon (Mac M-series). Core transcription engine.
# Non-Mac: replace usage with openai-whisper / faster-whisper.
mlx-whisper>=0.4

# Alibaba FunASR: SenseVoice (2nd ASR engine) + FSMN-VAD (vad_qa.py, mp4_to_lyrics --sensevoice)
funasr>=1.3

# torch + torchaudio are FunASR dependencies.
# IMPORTANT: install MATCHING versions (identical x.y.z), e.g. both 2.9.1,
# otherwise torchaudio fails to load.
torch
torchaudio

# audio loading / slicing for SenseVoice
librosa>=0.10
soundfile>=0.12

# Also required (system package, NOT pip):  ffmpeg
